#include <stdio.h>
#include "main.h"

/**
 * print_most_numbers - print numbers except 2 and 4
 *
 * Return: Nothing
 */
void print_most_numbers(void)
{
	int a;

	for (a = 48; a <= 58; a++)
	{
		if ((a == 50) || (n == 52))
		{
			continue;
		}
		_putchar(n);
	}
	_putchar(10);
}

