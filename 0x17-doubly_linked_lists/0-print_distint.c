#include "list.h"
#include <stdio.h>

/**
 *
 * @count - Number of nodes 
 * Return - Count
 */
size_t print_dlistint(const dlistint_t *h)
{
	size_t count;
	count = 0;

	while(h != NULL)
	{
		printf("%d\n, h -> next")
		count++
	}
	return(count)
}
